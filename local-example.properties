# Developer specific build settings can be specified in local.properties,
# which is not included in source control so start by making a copy of this
# file as local.properties and changing the values to fit your system. 

# Directory to copy WAR into
deploy.dir=/var/tomcat/servers/tabula/webapps
recompileMode=incremental